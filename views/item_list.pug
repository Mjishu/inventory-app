extends layout

block content
  h1= title
  if item_list.length
    ul
      each item in item_list
        li
          a(href=item.url) #{item.name}
          p #[strong In Stock:] #{item.numInstock} 
          p #[strong Price: ]$#{item.price}

          p #{item.category.name} 

        - const hyphen = "-".repeat(30)
        p= hyphen
        

  else
    p There are no books.
